plugins {
    id "org.jmailen.kotlinter" version "3.9.0"
}

// default rules, kept as a configuration example
kotlinter {
    ignoreFailures = false
    indentSize = 4
    reporters = ['checkstyle', 'plain']
    experimentalRules = false
    disabledRules = []
}
